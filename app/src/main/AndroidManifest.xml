<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.adrom.alibaba">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_alibaba"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_alibaba"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning,UnusedAttribute">

        <!-- android:networkSecurityConfig="@xml/network_security_config" -->
        <!-- android:usesCleartextTraffic="true" -->
        <!-- tools:ignore="GoogleAppIndexingWarning,UnusedAttribute" -->
        <!-- tools:targetApi="m" -->
        <activity
            android:name=".MainActivity"
            android:label="MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <receiver android:name=".Receiver.CheckInternetConnectionReceiver" /> -->
        <activity android:name=".CitiesActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".InformationActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".TrainInformationActivity" />
        <activity android:name=".BusInformationActivity" />
        <activity android:name=".ChairActivity" />
        <activity android:name=".HotelMapActivity"></activity>
    </application>

</manifest>